enum MockType {
  static,
  none,
}

class AppConstants {
  final MockType mocked = MockType.none;


  // Titles
  static const String appTitle = 'Probabilidade';

  //
  static const String zero = 'Zero';
  static const minAnalysis = 3;
  static const maxAnalysis = 999;
}
